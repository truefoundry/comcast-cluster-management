apiVersion: v1
kind: Service
metadata:
  name: {{ include "spark-job-fallback-management.fullname" . }}
  labels:
    {{- include "spark-job-fallback-management.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spark-job-fallback-management.selectorLabels" . | nindent 4 }}

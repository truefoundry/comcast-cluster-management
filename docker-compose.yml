version: '3.8'

services:
  app:
    build: .
    container_name: cluster-management-app
    ports:
      - "8000:8000"
    volumes:
      - app_data:/app/data
    environment:
      - DATA_DIR=/app/data
      - NODE_ENV=production
      # Uncomment when using PostgreSQL:
      # - DB_HOST=postgres
      # - DB_PORT=5432
      # - DB_USERNAME=admin
      # - DB_PASSWORD=admin123
      # - DB_NAME=cluster_management
    # depends_on:
    #   postgres:
    #     condition: service_healthy

  # ============================================================
  # PostgreSQL Database (Uncomment when ready to use)
  # ============================================================
  # postgres:
  #   image: postgres:16-alpine
  #   container_name: cluster-management-db
  #   environment:
  #     POSTGRES_USER: admin
  #     POSTGRES_PASSWORD: admin123
  #     POSTGRES_DB: cluster_management
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U admin -d cluster_management"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5

volumes:
  app_data:
  # postgres_data:  # Uncomment when using PostgreSQL

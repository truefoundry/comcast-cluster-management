version: '3.8'

services:
  app:
    build: .
    container_name: cluster-management-app
    ports:
      - "8000:8000"
    volumes:
      - app_data:/app/data
    environment:
      - DATA_DIR=/app/data
      - DB_DIALECT=sqlite
      - NODE_ENV=production

volumes:
  app_data:
